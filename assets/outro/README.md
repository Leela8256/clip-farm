Drop your company outro music here as `outro.mp3`.
The BrandMergeNode crossfades the episode end into it (800ms blend).
If missing, the pipeline skips the outro without erroring.

For local dev/testing, `outro.mp3` here is the same CC BY-NC-ND 3.0 jingle used for
`intro.mp3` (no dedicated outro track was found) — see assets/intro/README.md for
attribution. Replace with your own licensed brand audio before any real release.
