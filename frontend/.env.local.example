# Browser-side engine address + RocketRide API key (build-time; no secrets here).
# The Anthropic key is NOT configured in the frontend — set ROCKETRIDE_ANTHROPIC_KEY in the engine's environment.
NEXT_PUBLIC_ROCKETRIDE_URI=http://127.0.0.1:5567
NEXT_PUBLIC_ROCKETRIDE_APIKEY=MYAPIKEY
